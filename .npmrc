# Route the @bstockus scope to GitHub Packages. Auth token is NOT stored here —
# put `//npm.pkg.github.com/:_authToken=<PAT>` in your ~/.npmrc, or let CI supply
# NODE_AUTH_TOKEN via actions/setup-node.
@bstockus:registry=https://npm.pkg.github.com
